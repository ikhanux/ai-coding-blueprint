# PROJECT STATUS: [Project name]

**This is the single source of truth. Read this file first, every session.** Keep it
short on purpose, the full session-by-session narrative lives in `docs/history/`, one
file per session, indexed by `PROJECT_HISTORY.md`. Design/audit/reference docs live in
`docs/`.

> If this file ever grows past ~300-400 lines, that's a signal to move the "Recent
> sessions" digest into `docs/history/` and leave only a short bullet + current-state
> edit here. A file that long stops getting read, and gaps get filled by guessing.

## Phases

The whole project, broken into recognizable chunks, see `docs/HOW_TO_OPERATE.md`
Job 1 for how to use this. Sessions are named after whichever phase they fall in
(e.g. "Phase 2 Session 7"); the session number keeps counting up across the whole
project, it does not reset per phase.

| Phase | What it covers | Status |
|-------|-----------------|--------|
| Phase 1: [name] | [what "done" means for this phase] | [not started / in progress / done] |
| Phase 2: [name] | | |

## Recent sessions (short version)

One bullet per session, newest first, last five only, named "Phase N Session M".
Older sessions are in `PROJECT_HISTORY.md`.

- **Phase N Session M ([date]):** [One or two sentences, what happened, what
  shipped, what's still open. Flag anything surprising with a note.]

## Current state

| Area | Status | Notes |
|------|--------|-------|
| [e.g. Deploy] | [e.g. Green] | [Last deployed commit, when] |
| [e.g. Tests] | [e.g. 42 passing] | [How to run them] |
| [e.g. Open blockers] | [ ] | [What's blocking, who/what it's waiting on] |

## Settled decisions: do not re-raise

[Anything the user has decided firmly enough that re-litigating it wastes their time.
Delete this section if nothing qualifies yet.]

## Appendices

[Links to design docs, runbooks, audits living in docs/.]
